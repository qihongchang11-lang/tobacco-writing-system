# 云端部署专用环境变量配置
# 这些配置会通过云平台的环境变量设置

# Claude API (必须通过云平台环境变量设置)
CLAUDE_API_KEY=""
CLAUDE_MODEL="claude-3-sonnet-20241022"

# 简化的配置（云端优化）
LOG_LEVEL="INFO"
MAX_CONTENT_LENGTH="10000"

# 云端存储路径（临时目录）
CHROMA_DB_PATH="/tmp/chroma_db"
FAISS_INDEX_PATH="/tmp/faiss_index"

# Web配置
HOST="0.0.0.0"
PORT="8501"
DEBUG="False"

# 质量阈值
QUALITY_THRESHOLD="0.7"